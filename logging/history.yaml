  include:
    domains:
      - sun
      - automation
      - light
      - switch
      - binary_sensor
      - device_tracker
      - script
    #entities:
  exclude:
    domains:
      - camera
      - media_player
      - calendar
      - sensor
      - group
    #entities:
      # - sensor.since_last_boot
      # - sensor.last_boot
      # - sensor.date__time
      # - sensor.date
      # - sensor.time
